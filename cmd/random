if (is_numeric($value) && is_numeric(trim($ex[6]))) {
	send("Randomly chosen number between {$value} and ".trim($ex[6]).": ".rand($value, $ex[6]));
}else{
	send("Randomly chosen number between 1 and 1000: ".rand(0, 1000));
}